# App
PORT=3001
JWT_SECRET=super-secret-change-me
JWT_EXPIRES_IN=7d

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres
DB_NAME=time_tracker

# Mail (без SMTP_HOST письма логируются в консоль — dev-режим)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM=Хронос <no-reply@chronos.local>
# База для ссылок в письмах
FRONTEND_URL=http://localhost:3000

# Генератор текстов
# AI-сводка в стендапе через Claude API (без ключа — фолбэк «задача — часы»)
ANTHROPIC_API_KEY=
# Вебхуки доставки стендапа (без них отправка в демо-режиме)
SLACK_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
